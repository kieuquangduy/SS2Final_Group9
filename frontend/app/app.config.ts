export default defineAppConfig({
  foo: 'bar',
  ui: {
    input: {
        variants: {
            size: {
                md: {
                    base: 'text-base'
                }
            }
        }
    }
  }
})
